meta {
  name: Profile
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/auth/profile/dashboard
  body: json
  auth: bearer
}

auth:bearer {
  token: {{userAccessToken}}
}

body:json {
  {
      "settings": {
          "receiveEmailNotifications": true
      }
  }
}

settings {
  encodeUrl: true
}
