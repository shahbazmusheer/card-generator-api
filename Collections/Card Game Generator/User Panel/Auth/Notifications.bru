meta {
  name: Notifications
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/notifications
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userAccessToken}}
}

settings {
  encodeUrl: true
}
